# Server Configuration
PORT=9998
NODE_ENV=production
HOST=0.0.0.0

# CORS Configuration
# Comma-separated list of allowed origins, use '*' to allow all origins
# For production, specify exact origins: http://example.com,https://example.com
ALLOWED_ORIGINS=*

# Database Configuration (optional - defaults to ./database/auth.db)
# DB_PATH=/app/database/auth.db

# Production Example:
# PORT=9998
# NODE_ENV=production
# HOST=0.0.0.0
# ALLOWED_ORIGINS=http://your-main-app-domain.com,https://your-main-app-domain.com
# DB_PATH=/app/database/auth.db
